# Install a CUDA-compatible torch/torchvision build first when GPU support is needed.
torch>=2.0
torchvision>=0.15
monai>=1.3
timm>=0.9
nibabel>=5.0
numpy>=1.23
pandas>=1.5
scipy>=1.9
scikit-learn>=1.2
matplotlib>=3.7
opencv-python-headless>=4.8
Pillow>=9.5
openpyxl>=3.1
# Optional: the prognosis code includes a fallback when lifelines is unavailable.
lifelines>=0.27

